

InitKeyboard()

Drive = drive(brick, 'A', 'D');
Lift = lift(brick, 'B', 425);
Ultra = ultrasonic(brick, 1, 50);
Color = color(brick, 2);

speedMultiplier = 1;
modeFlag = 0;

while true
    pause(0.01)
    switch key
        
    end
end

CloseKeyboard();