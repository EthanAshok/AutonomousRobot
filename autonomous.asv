function autonomous(Drive, Lift, Ultra, Color)
%AUTONOMOUS Summary of this function goes here
%   Detailed explanation goes here
    display(Ultra.getDist(Ultra))

    switch true
        case brick.ColorCode(nos_color) == 5
            move(0)
            pause(3) 
        case (distance > 30)
            move(-1)
        case (distance < 30)
            move(0)
            pause(0.2)
            turnDeg(1, 1, 360 )
        otherwise
            move(0)
    end
end

